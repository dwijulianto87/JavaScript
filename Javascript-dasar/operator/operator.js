// Operator
let a ;
let b ;
let c;

//1. binary -> membutuhkan 2 operand (operand1 operator operand2) /////
//1.A operator aritmatika/ matematika (*, /, +, -, %) -> (binary)**
//operator precedence (urutan operasi matematika -> kurkabataku)
// a = 2 + 3;



//operator assignment/ penugasan  (=, *=, /=, +=, -=, %=) -> (binary)**
// a = 10;



//operator comparison/ perbandingan (==, !=, ===, !==, <, >, <=, >=) -> (binary)**
//10 < 15 ----> true
//10 > 15 ----> false




//operator logical/ logika (&&, ||, !) -> (binary)**
//(kondisi) && (kondisi) --> true/ false
// a = (5>3) && (4>2);



//operator string (binary)**
// concatenation (+);
// a = 'pisang' + ' goreng';
// b = "hari jum'at diadakan \"bakti sosial\" bersama"



//operator kondisional (ternary)***
//membutuhkan 3 operand (operand1 operator operand2)
// (kondisi)? 'true' : 'false';
// (a % 2 == 0)? 'genap' : 'ganjil';



//operator typeof (typeof(operand)) -> (unary)*
//membutuhkan 1 operand (operand operator atau operator operand)
// a = 10;
// b = 'ini string';
// c = true;
// typeof(a); ----> number
// typeof(b); ----> string
// typeof(c); ----> boolean
// Boolean(a); ---> true

// truthy(true) | falsy(false)



// operator increment (++) dan decrement (--). Operator increment dan decrement digunakan untuk menambahkan atau mengurangi nilai 1 pada nilai variabel yang ada sekarang.

// Operator ini dapat dituliskan sebelum atau sesudah variabel, tetapi hal tersebut bukan berarti sama. Berikut ketentuannya:

// Jika dituliskan setelah variabel (x++), statement akan menghasilkan nilai variabel sebelum ditingkatkan nilainya.
// Jika dituliskan sebelum variabel (++x), statement akan menghasilkan nilai variabel setelah ditingkatkan nilainya.